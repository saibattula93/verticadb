apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: verticadb-pvc
  namespace: vertica
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
