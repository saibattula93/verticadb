apiVersion: v1
kind: PersistentVolume
metadata:
  name: verticadb-pv
spec:
  storageClassName: rook-ceph-block
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  local:
    path: /mnt/home/test/vertica-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - cloudlyte
